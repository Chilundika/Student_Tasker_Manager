document.addEventListener('DOMContentLoaded', function() {
    fetchTasks();
    fetchSubjects();
});

function fetchTasks() {
    // Fetch tasks from the server and display them
}

function fetchSubjects() {
    // Fetch subjects from the server and display them
}
